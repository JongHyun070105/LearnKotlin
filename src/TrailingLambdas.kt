//fun main() {
//    // 초기값은 0
//    // 초기값과 목록의 모든 항목의 값을 누적하여 합산한다.
//    println(listOf(1, 2, 3).fold(0, { x, item -> x + item })) // 6
//
//    // 후행 람다 형태
//    println(listOf(1, 2, 3).fold(0) { x, item -> x + item })  // 6
//}